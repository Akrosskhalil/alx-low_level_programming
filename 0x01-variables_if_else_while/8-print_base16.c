#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print base hexa'
 * Return: Always 0
 */
int main(void)
{
	int n;
	char m;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (n = 97; m <= 102; m++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
